# 지역별 맛집 조회 서비스 — PRD & 구현 설계서

| 항목 | 내용 |
|---|---|
| 서비스명 (가칭) | **동네한끼** |
| 문서 버전 | **v0.2** — 초안 설계서 |
| 작성일 | 2026-08-19 |
| 작성자 | 조예원 |
| 용도 | 직접 개발용 설계서 |
| 형태 | 웹 (Next.js App Router + 자체 DB + 카카오 로컬 API) |

**v0.1 → v0.2 변경사항**
- **F7 "내 주변 맛집" 신설** — 카카오 로컬 API(카테고리 검색) 기반, 현재 위치 실시간 조회 (§4.6, §6-F7)
- 데이터 출처가 **이원화**됨: 지역선택 목록(F1~F3)은 여전히 공공데이터 ETL, 주변찾기(F7)는 카카오 실시간 호출. 서로 다른 신선도·정확도를 화면에서 구분 표기 (§4.6)
- Non-goals에서 "지도 기반 반경 검색"을 제거하고 F7로 승격, 로드맵 v1.1 → **v1.0에 포함**

**확정된 설계 결정**

| # | 항목 | 결정 |
|---|---|---|
| 1 | 데이터 조달 (지역목록) | **배치 ETL → 자체 DB** (런타임 프록시 아님). 근거는 §4.4 |
| 2 | 데이터 조달 (주변찾기) | **카카오 로컬 API 실시간 호출** — 별도 소스, DB 병합 안 함 (§4.6) |
| 3 | 지역 선택 | **시·도 → 시·군·구 2단 캐스케이딩** |
| 4 | 카테고리 | 원본 업종명을 **9개 표준 카테고리로 정규화** (§5.3), 카카오 결과도 동일 체계로 매핑 (§4.6) |
| 5 | 목록 | 서버 페이지네이션 (20건/페이지) — 주변찾기는 페이지네이션 없음(최대 45건 표시) |
| 6 | 지도 | 지역목록 상세는 v1 그대로. **주변찾기는 지도가 메인 뷰** |
| 7 | URL | `/seoul/gangnam-gu?cat=korean` — 공유·SEO 가능한 경로. 주변찾기는 `/nearby` (좌표는 URL에 노출 안 함) |

---

## 1. 배경 & 문제 정의

"강남구에서 일식 먹을 데"를 찾는 일은 이미 포털·지도 앱이 다 한다. 그런데 이 서비스가 겨냥하는 지점은 그것들이 잘 못하는 쪽에 있다.

- **개인화된 추천이 개입한다** — 포털 지도는 광고·순위·리뷰 점수가 섞여 있어 "이 동네에 무엇이 있는가"라는 **목록으로서의 완결성**을 주지 않는다. 스크롤을 내려도 끝이 어디인지 모른다.
- **지역을 '선택'할 수 없다** — 대부분 현재 위치나 지도 뷰포트 기준이다. "다음 달 갈 통영, 거기 한식집 목록"을 보려면 지도를 옮기고 확대·축소를 반복해야 한다.
- **리뷰가 없으면 아무것도 없다** — 리뷰 수가 적은 지방 소도시일수록 정보가 급격히 빈약해진다. 공공데이터는 반대로 **전국을 균일하게 덮는다**.

**해결 가설**: 광고도 추천 알고리즘도 없이, **시·도 → 시·군·구 → 음식 카테고리** 세 번의 선택만으로 "그 지역, 그 종류의 식당 전체 목록"을 정직하게 보여준다. 순위를 매기지 않는 대신 **빠짐이 없다.** 여행 계획, 출장, 낯선 동네 탐색처럼 "추천"보다 "지도 위의 목록"이 필요한 순간을 노린다.

> 이 서비스는 맛집 **평가** 서비스가 아니다. 공공데이터에는 평점이 없고, 만들어 넣지도 않는다. 이 경계를 흐리면 "정보가 부실한 맛집 앱"이 된다. 우리가 파는 것은 **커버리지와 명료함**이다.

---

## 2. 목표

**제품 목표**

1. 첫 진입 후 **3번의 클릭 이내**에 원하는 지역·카테고리의 식당 목록에 도달한다.
2. 목록은 **전수**다 — "상위 몇 개"가 아니라 총 건수를 밝히고 전부 페이지네이션한다.
3. **URL이 곧 결과다** — `/busan/haeundae-gu?cat=japanese`를 복사해 보내면 상대가 같은 화면을 본다.
4. 데이터의 **한계를 숨기지 않는다** — 기준일, 출처, 폐업 가능성을 화면에 적는다.

**Non-goals (이번 범위 아님)**

- 로그인 / 계정 / 즐겨찾기 서버 동기화 (localStorage 즐겨찾기는 v1.1)
- 평점·리뷰·순위 — 데이터에 없는 것을 만들지 않는다
- 예약·주문·결제 연동
- 실시간 영업 여부 — 공공데이터 목록(F1~F3)에는 없다(원본에 필드 없을 가능성 → §14-B). **단, 카카오 결과(F7)에는 영업시간·영업중 여부가 포함될 수 있어 별도 처리** (§4.6)
- ~~지도 기반 반경 검색~~ → **F7로 승격, v1.0에 포함** (v0.2)
- 사용자 제보/편집 — v1.2

---

## 3. 타겟 사용자

| | 내용 |
|---|---|
| 대표 페르소나 | 여행·출장을 앞둔 20~40대. 목적지가 정해져 있고, **그 지역에 뭐가 있는지 전체를 훑고 싶다** |
| 사용 맥락 | 여행 계획 단계(출발 며칠 전, PC) / 낯선 도시 도착 직후(모바일) |
| 원하는 것 | 지역 + 종류로 좁힌 **목록**, 전화번호, 주소, 위치 |
| 원하지 않는 것 | 로그인, 광고성 상단 노출, 무한스크롤 속 미아 상태 |

**설계 원칙**: 선택 상태(어디·무엇)가 **항상 화면 상단에 보이고, 항상 되돌릴 수 있어야** 한다. 목록형 서비스의 최대 이탈 요인은 "내가 지금 뭘 보고 있는지 모르는" 상태다.

---

## 4. 데이터 소스 — 문화공공데이터광장 오픈API

### 4.1 사용 API

문화공공데이터광장(한국문화정보원)이 제공하는 **음식점 정보 오픈API**를 사용한다. 사용자가 지정한 Swagger:

```
https://www.culture.go.kr/data/openapi/getSwagger.do?openApiId=75493ae2-…&id=603
```

| 항목 | 내용 |
|---|---|
| 제공 기관 | 한국문화정보원 (문화공공데이터광장) |
| 데이터 성격 | 전국 음식점 현황 — 상호, 주소, 좌표, 전화, 업종 분류 |
| 인증키 | 문화공공데이터광장 활용신청 (자동승인 계열) |
| 호출 한도 | **개발계정 일 1,000건**, 운영계정 전환 시 확대 |
| 응답 형식 | JSON / XML |
| 갱신 주기 | 정적에 가까움 (분기~연 단위 추정) |

> ⚠️ **이 문서의 API 세부 규격은 확정이 아니다.** 문화공공데이터광장은 자동 접근을 차단하고 있어 Swagger 원문을 문서 작성 시점에 열람하지 못했다. 아래 §4.2·§4.3은 **동일 계열 API(`api.kcisa.kr`)와 같은 원천 데이터셋의 알려진 규격**을 근거로 한 작업 가정이며, **구현 순서 1번의 실호출로 반드시 검증·교체한다**(§14-A). 이 검증이 끝나기 전에는 파서 이후 코드를 쓰지 않는다.

### 4.2 요청 파라미터 (가정)

```http
GET https://api.kcisa.kr/openapi/{서비스경로}
  serviceKey  (필수)  발급 인증키 — URL 인코딩된 키 사용 주의
  numOfRows   1000    # 한 페이지 결과 수
  pageNo      1
  # 아래는 존재 여부 미확인 — §14-A에서 확정
  # keyword   상호/지역 키워드
```

**주의**: `serviceKey`는 이중 인코딩 사고가 잦다. 포털이 주는 "Encoding 키"를 그대로 쓸지, "Decoding 키"를 코드에서 인코딩할지 **한쪽으로 통일**하고 주석으로 명시한다.

### 4.3 응답 필드 (가정)

| 필드 | 의미 | 사용처 |
|---|---|---|
| `RSTR_ID` | 음식점 고유 ID | PK, 중복 제거 |
| `RSTR_NM` | 상호명 | 목록·상세 제목 |
| `RSTR_RDNMADR` | 도로명주소 | **지역 파싱 1순위**, 표시 |
| `RSTR_LNNO_ADRES` | 지번주소 | 지역 파싱 폴백, 표시 |
| `RSTR_LA` / `RSTR_LO` | 위도 / 경도 | 지도 마커 |
| `RSTR_TELNO` | 전화번호 | 상세, 모바일 통화 링크 |
| `BSNS_STATM_BZCND_NM` | 업태명 (한식·중식·일식·기타 등) | **카테고리 매핑 핵심** |
| `BSNS_LCNC_NM` | 인허가 업종 (일반음식점·휴게음식점) | 보조 필터 (v1.1) |
| `RSTR_INTRCN_CONT` | 음식점 소개 | 상세 본문 |
| `AREA_NM` | 지역명 (있을 수도) | 있으면 지역 파싱 1순위로 승격 |

**예상되는 함정들**

- **좌표가 문자열이고 결측이 흔하다.** `""`, `"0"`, `"0.0"`이 모두 "없음"을 뜻할 수 있다. `Number()` 후 대한민국 경위도 범위(위도 33~39, 경도 124~132)를 벗어나면 **좌표 없음으로 확정**한다.
- **전화번호 표기가 제각각이다** — `02-725-0062`, `0227250062`, `02)725-0062`. 저장은 정규화, 표시는 하이픈 포맷.
- **상호명에 지점명·특수문자가 섞인다** — `"펜앤커피"` 처럼 따옴표가 포함된 경우가 실제로 보고된다. 트림·따옴표 제거 필요.
- **소개문이 템플릿 문장이다** — `"OO는 기타 맛집입니다"` 같은 자동 생성 문구가 다수. 이런 문장은 **상세에 그대로 노출하지 않는다**(정보량 0인데 신뢰만 깎는다). 템플릿 패턴을 감지해 숨긴다.
- **폐업 매장이 남아 있다.** 공공데이터의 숙명이다. 화면에 기준일을 상시 노출하고, 전화 연결 실패를 전제로 안내한다.

### 4.4 ★ 핵심 제약 — "선택 후 조회"가 API로는 성립하지 않는다

이 서비스의 요구사항은 **시·도 + 시·군·구 + 카테고리로 필터링된 목록**이다. 그런데 이 계열 API는 통상 `pageNo`/`numOfRows` 외에 **지역·업종 필터 파라미터를 제공하지 않는다.** 즉 "강남구 일식만 주세요"라는 요청 자체가 불가능하다.

세 가지 대안을 놓고:

| 방식 | 채택 | 판단 |
|---|---|---|
| **A. 런타임 프록시 + 전체 순회 필터** | ❌ | 요청 1건마다 전체 데이터를 훑어야 한다. 일 1,000건 한도를 한 사용자가 몇 분 만에 소진한다. 응답 시간도 수십 초 |
| **B. 배치 ETL → 자체 DB** | ✅ **채택** | 조회는 우리 DB의 인덱스 질의(10ms). 호출 한도와 사용자 트래픽이 **완전히 분리**된다. 지역·카테고리 정규화를 적재 시점에 한 번만 하면 된다 |
| C. 전체 JSON 정적 번들 | ❌ | 전국 수십만 행 = 수십 MB. 클라이언트가 감당 못 한다. 시·군·구별 분할해도 파일 수천 개 |

**B를 택한 순간 따라오는 것들** — 정직하게 적어둔다.

- 데이터가 **실시간이 아니다.** 원본이 분기 단위로 갱신되는 정적 데이터이므로 실질 손실은 거의 없지만, **"기준일"을 화면에 반드시 표기**한다.
- **DB 운영 부담이 생긴다.** 대신 Next.js 하나로 끝나던 구조에 DB가 붙는다 → §9에서 서버리스 Postgres를 택해 운영 부담을 최소화.
- **ETL 실패가 서비스 전체 정지로 이어지지 않는다** — 기존 스냅샷이 계속 서빙되기 때문이다. 이건 오히려 A안 대비 **가용성 이점**이다.

**적재 규모 산정**

```
전국 음식점 ≈ 15~20만 행 (추정, §14-A에서 totalCount로 확정)
numOfRows=1000 → 150~200 페이지
= 하루 호출 한도(1,000건)의 20% 이내로 전량 수집 가능
→ 초기 적재 1회 + 주 1회 갱신으로 충분. 운영계정 전환도 급하지 않다
```

### 4.5 ETL 파이프라인

```
[GitHub Actions — 주 1회 / 수동 트리거]
   │  ① totalCount 조회 → 페이지 수 계산
   │  ② 1페이지씩 순차 수집 (동시 요청 금지, 200ms 간격)
   │     · 실패 페이지는 3회 재시도(지수 백오프), 그래도 실패하면 기록 후 계속
   │  ③ 원본 응답을 그대로 아카이브 (raw/YYYY-MM-DD.jsonl)  ← 재처리·디버깅용
   ▼
[정규화]  ① 주소 → sido / sigungu (§5.2)
          ② 업태명 → category (§5.3)
          ③ 좌표·전화 정제, 템플릿 소개문 플래그
   ▼
[검증 게이트]  ★ 통과 못 하면 반영하지 않는다
   · 총 건수가 직전 스냅샷의 ±20% 밖 → 중단
   · 시·군·구 미매핑률 > 3% → 중단
   · 카테고리 미매핑('기타') 비율 > 25% → 중단
   ▼
[스테이징 테이블 적재 → 원자적 스왑]
   · 새 스냅샷을 별도 테이블에 채운 뒤 트랜잭션으로 교체
   · 서비스가 반쯤 적재된 데이터를 읽는 구간이 없다
```

> **검증 게이트가 이 파이프라인의 핵심이다.** 공공 API가 어느 날 스키마를 바꾸거나 일부만 응답하면, 게이트 없이는 "전국 식당이 3만 개로 줄어든" 서비스가 조용히 배포된다. 깨진 데이터를 서빙하느니 **지난주 데이터를 계속 서빙**하는 편이 낫다.

### 4.6 카카오 로컬 API — "내 주변 맛집"(F7) 전용 데이터원

F1~F3(지역 선택 목록)과 F7(내 주변 맛집)은 **의도적으로 데이터 출처를 분리**한다. 하나로 합치려는 유혹이 있지만, 두 기능의 신뢰 약속이 다르기 때문에 섞으면 둘 다 망가진다.

| | F1~F3 지역목록 | F7 내 주변 맛집 |
|---|---|---|
| 출처 | 공공데이터 ETL → 자체 DB | 카카오 로컬 API 실시간 |
| 신뢰 약속 | **전수** — "이 지역 전체" | **근접성** — "지금 여기서 가까운 곳" |
| 신선도 | 주 1회 배치 (기준일 표기) | 실시간 |
| 커버리지 상한 | 없음 (전량) | **최대 45건** (API 자체 제한) |
| 저장 | DB 영속 저장 | **저장하지 않음** — 요청마다 재조회 (§4.6-라이선스) |

**왜 병합하지 않는가**: 카카오 결과를 공공데이터 DB에 합치면 "전수 목록"이라는 F1~F3의 약속이 깨진다(카카오는 45건이 상한이라 병합 즉시 상한이 전체 상한이 됨). 반대로 공공데이터를 F7에 섞으면 "지금 이 순간"이라는 실시간성이 깨진다. **화면에서도 출처를 명확히 구분 표기**한다 — "카카오맵 제공" 배지 vs "공공데이터 기준 YYYY-MM-DD".

**API 호출 방식**

```http
GET https://dapi.kakao.com/v2/local/search/category.json
  category_group_code = FD6        # 음식점. 카페 별도 토글 시 CE7
  x, y                              # 사용자 좌표 (경도, 위도) — 브라우저 Geolocation
  radius                            # 500 / 1000 / 2000m 중 사용자 선택, 기본 1000
  sort                              # distance (거리순 고정 — 평점이 없으므로 유일하게 정직한 정렬)
  page, size                        # size=15 고정, page 1~3 (45건 상한)
Header: Authorization: KakaoAK {KAKAO_REST_API_KEY}
```

**반드시 서버 프록시를 거친다** (`/api/nearby`) — 클라이언트에 REST 키를 노출하지 않고, IP·좌표 단위로 짧은 캐시(60초, geohash 6자리 버킷)를 걸어 동일 위치 연타 요청이 쿼터를 갉아먹지 않게 한다. 카카오 로컬 API는 무료 쿼터가 넉넉하지만(일 30만 건 내외), 사용자 수가 늘면 서버 프록시가 유일한 통제 지점이 된다.

**카카오 카테고리 → 내부 표준 카테고리 매핑**

카카오는 `category_name`을 `"음식점 > 한식 > 육류,고기"`처럼 계층 문자열로 준다. §5.3의 정규식 규칙을 **그대로 재사용**할 수 있다 — 카카오 응답에서 마지막 `>` 이후 텍스트를 뽑아 동일 `toCategory()` 함수에 넣으면 F1~F3과 **같은 9개 카테고리 체계**로 보인다. 사용자 입장에서 카카오 출처인지 공공데이터 출처인지와 무관하게 카테고리 필터 UI가 동일하게 동작해야 한다.

```ts
function kakaoCategoryToStandard(categoryName: string): Category {
  const leaf = categoryName.split('>').pop()?.trim() ?? '';
  return toCategory(leaf);   // §5.3의 동일 함수 재사용
}
```

**라이선스·정책 제약 (반드시 지킬 것)**

- 카카오 로컬 API 이용약관상 검색 결과의 **영구 저장·재배포는 금지**에 가깝다 — 캐시는 초 단위 TTL로만 두고 DB 테이블에 쌓지 않는다. (§4.6은 §4.4의 ETL·영속 저장 원칙과 **의도적으로 다른 규칙**을 따른다)
- 결과에 **"제공: 카카오"** 표기와 `place_url`로의 링크 노출을 유지한다.
- 지도를 함께 쓸 경우 카카오맵 JS SDK 사용을 권장 — 카카오 로컬 API로 받은 장소를 타 지도(구글맵 등)에 표시하는 것은 약관상 회색지대이므로 피한다.

**위치 권한 예외 처리**

| 상황 | 처리 |
|---|---|
| Geolocation 권한 거부 | "위치 권한이 필요해요" + 지역 직접 선택(F1)으로 유도하는 버튼 |
| HTTPS 아님 | 브라우저가 API 자체를 막음 → 배포 환경 필수 확인 |
| 좌표는 얻었으나 결과 0건 | 반경을 자동으로 1단계 넓혀 재조회(최대 2000m) 후에도 0건이면 "주변에 결과가 없어요" |
| 카카오 API 오류/타임아웃(3초) | "지금은 주변 정보를 불러올 수 없어요" + 지역 선택(F1)으로 폴백 |

---

## 5. 핵심 로직

### 5.1 데이터 흐름

```
[GitHub Actions]  주 1회 ETL ──────────┐
                                        ▼
[문화공공데이터광장 API]          [Postgres — restaurants]
                                        │  인덱스: (sido, sigungu, category)
                                        ▼
                                 [Next.js Route Handler]
                                        │  GET /api/restaurants?…
                                        ▼
                                   [브라우저 SPA]
```

인증키는 **ETL 워크플로의 시크릿에만** 존재한다. 웹 서버는 공공 API를 아예 호출하지 않으므로, 런타임에 키가 유출될 경로 자체가 없다.

### 5.2 주소 → 시·도 / 시·군·구 파싱

전체 파이프라인에서 **가장 자주 깨지는 지점**이다. 한국 행정구역은 예외가 많다.

**함정 목록**

| 케이스 | 예시 | 처리 |
|---|---|---|
| 광역시도 표기 흔들림 | `서울특별시` / `서울시` / `서울` | 정규화 테이블로 17개 표준값에 매핑 |
| 특별자치 | `강원특별자치도`, `전북특별자치도`, `제주특별자치도` | 각각 강원/전북/제주로 정규화 |
| **세종** | `세종특별자치시 한누리대로 …` | **시·군·구가 없다.** `sigungu = '세종시'` 단일값으로 고정 |
| **일반시 산하 구** | `경기도 수원시 영통구`, `충북 청주시 상당구` | 시·군·구 단위를 **`수원시 영통구`처럼 2어절로** 저장. `수원시`로 뭉치면 목록이 너무 크고, `영통구`만 쓰면 `중구`가 전국에 여럿이라 충돌 |
| 군 단위 | `경북 울릉군` | 그대로 |
| 주소 결측 | 도로명·지번 모두 빈 값 | **적재 제외** — 지역을 모르는 식당은 이 서비스에서 존재 의미가 없다. 제외 건수는 로그로 남긴다 |

```ts
const SIDO_ALIAS: Record<string, Sido> = {
  '서울특별시': '서울', '서울시': '서울', '서울': '서울',
  '부산광역시': '부산', '대구광역시': '대구', '인천광역시': '인천',
  '광주광역시': '광주', '대전광역시': '대전', '울산광역시': '울산',
  '세종특별자치시': '세종',
  '경기도': '경기', '강원특별자치도': '강원', '강원도': '강원',
  '충청북도': '충북', '충청남도': '충남',
  '전북특별자치도': '전북', '전라북도': '전북', '전라남도': '전남',
  '경상북도': '경북', '경상남도': '경남',
  '제주특별자치도': '제주', '제주도': '제주',
};

// 일반시 산하 구를 가진 시 — 2어절로 묶어야 하는 대상
const CITIES_WITH_GU = new Set([
  '수원시', '성남시', '안양시', '안산시', '고양시', '용인시',
  '청주시', '천안시', '전주시', '포항시', '창원시',
]);

function parseRegion(road: string, jibun: string): Region | null {
  const addr = (road?.trim() || jibun?.trim() || '');
  if (!addr) return null;

  const parts = addr.split(/\s+/);
  const sido = SIDO_ALIAS[parts[0]];
  if (!sido) return null;                      // 미매핑 → 게이트 지표에 집계

  if (sido === '세종') return { sido, sigungu: '세종시' };

  const first = parts[1];
  if (!first) return null;

  // '수원시 영통구' 형태로 결합
  if (CITIES_WITH_GU.has(first) && parts[2]?.endsWith('구')) {
    return { sido, sigungu: `${first} ${parts[2]}` };
  }
  if (/(시|군|구)$/.test(first)) return { sido, sigungu: first };

  return null;
}
```

> **`CITIES_WITH_GU`를 하드코딩하지 않는 방법도 있다** — 행정표준코드(법정동코드) 테이블을 별도로 싣고 조인하는 것이다. 정확하지만 v1에는 과하다. 미매핑률이 게이트 임계(3%)를 넘기면 그때 도입한다(§14-C).

### 5.3 업태명 → 표준 카테고리 매핑

원본 `BSNS_STATM_BZCND_NM`은 자유 텍스트에 가깝다. `한식`, `한식일반`, `경양식`, `기타`, `호프/통닭` 같은 값이 섞여 들어온다. 이걸 그대로 칩으로 만들면 카테고리가 수십 개가 되고, 사용자는 `한식`과 `한식일반` 중 뭘 눌러야 할지 모른다.

**표준 8종 + 기타**

| 카테고리 | slug | 매핑되는 원본 예시 |
|---|---|---|
| 한식 | `korean` | 한식, 한식일반, 백반, 국밥, 분식 일부 |
| 중식 | `chinese` | 중식, 중국식 |
| 일식 | `japanese` | 일식, 일식일반, 초밥, 횟집 |
| 양식 | `western` | 양식, 경양식, 이탈리아식, 패밀리레스토랑 |
| 아시안·기타 외국식 | `asian` | 동남아, 인도, 베트남, 태국 |
| 분식 | `snack` | 분식, 김밥, 떡볶이 |
| 카페·디저트 | `cafe` | 커피숍, 제과점, 아이스크림 |
| 술집 | `bar` | 호프, 주점, 바, 통닭 |
| 기타 | `etc` | 위 어디에도 안 걸리는 값 |

```ts
// 우선순위 있는 규칙 배열 — 위에서부터 첫 매칭
const CATEGORY_RULES: [RegExp, Category][] = [
  [/카페|커피|제과|디저트|아이스크림|베이커리/, 'cafe'],
  [/호프|주점|술집|바\b|포차|통닭/,             'bar'],
  [/분식|김밥|떡볶이/,                          'snack'],
  [/중식|중국/,                                 'chinese'],
  [/일식|초밥|스시|횟집|회집/,                  'japanese'],
  [/양식|경양식|이탈리|프렌치|스테이크|피자|패밀리/, 'western'],
  [/베트남|태국|인도|동남아|아시안|외국/,        'asian'],
  [/한식|백반|국밥|고기|곰탕|칼국수|족발|보쌈/,  'korean'],
];

function toCategory(raw?: string): Category {
  const s = (raw ?? '').replace(/\s/g, '');
  for (const [re, cat] of CATEGORY_RULES) if (re.test(s)) return cat;
  return 'etc';
}
```

**운영 규칙**

- ETL 때 `etc`로 떨어진 **원본 값과 건수를 리포트로 출력**한다. 상위 값을 보고 규칙을 추가하는 것이 이 매핑을 개선하는 유일한 방법이다.
- 카페·술집이 `호프` 같은 값으로 `korean` 규칙보다 먼저 잡혀야 하므로 **순서가 의미를 가진다.** 규칙을 추가할 때 위치를 잘못 잡으면 조용히 오분류된다 → 단위 테스트에 대표 케이스 20개 고정.
- **`etc` 카테고리도 화면에 노출한다.** 숨기면 목록의 전수성이 깨진다.

### 5.4 정렬 & 중복

- 기본 정렬: **상호명 가나다순**. 평점이 없으므로 "추천순"을 만들 수 없고, 만들어서도 안 된다.
- 보조 정렬(v1.1): 좌표가 있을 때 지도 중심으로부터의 거리.
- 중복 제거: `RSTR_ID` 기준. ID가 없으면 `해시(정규화 상호명 + 도로명주소)`.
- 좌표 없는 레코드도 **목록에는 포함**한다(주소·전화만으로도 유용). 지도에서만 빠진다.

---

## 6. 기능 명세 & 수용 기준

### F1. 지역 선택 — 시·도 → 시·군·구 (P0)

```
┌─────────────────────────────────────────────┐
│  어디서 드시나요?                            │
│                                             │
│  [서울 ▾]   [강남구 ▾]                       │
│                                             │
│  서울 · 강남구  ×                            │
└─────────────────────────────────────────────┘
```

- 1단: 17개 시·도. 2단: 선택된 시·도의 시·군·구만 로드
- 시·도를 바꾸면 시·군·구는 **초기화**된다(다른 시·도의 구가 남아 있으면 안 됨)
- 각 항목에 **해당 지역 식당 수**를 함께 표기 — `강남구 (1,204)`. 0건인 지역은 목록에서 제외
- 모바일은 드롭다운이 아니라 **바텀시트 목록** (터치 타깃 44px 이상)
- 선택 결과는 상단에 **breadcrumb 칩**으로 상시 노출, `×`로 해제

**수용 기준**

- [ ] 시·도 선택 → 시·군·구 옵션이 300ms 이내 갱신
- [ ] 시·도 변경 시 이전 시·군·구 선택이 확실히 해제됨
- [ ] 세종 선택 시 2단이 `세종시` 단일 항목으로 뜨거나 자동 선택되어 막다른 길이 없음
- [ ] `<select>` 또는 `role="listbox"` + 키보드(↑↓, Enter, Esc) 조작 가능
- [ ] URL `/gyeonggi/suwon-si-yeongtong-gu` 직접 입력·새로고침해도 선택 상태 복원
- [ ] 잘못된 슬러그는 400이 아니라 **선택 화면으로 폴백**하고 "해당 지역을 찾지 못했어요" 안내

### F2. 음식 카테고리 선택 (P0)

- 9개 카테고리(§5.3)를 **칩**으로 한 화면에 노출. 드롭다운이 아니다 — 선택지가 적고 고정적이므로 펼쳐두는 편이 빠르다
- 각 칩에 **현재 지역 기준 건수** 표기: `일식 (86)`. 0건 카테고리는 **비활성**(숨기지 않는다 — 없다는 사실도 정보다)
- **다중 선택 가능** (한식+분식). 미선택 = 전체
- 선택 즉시 목록 갱신. 페이지 이동 없음

**수용 기준**

- [ ] 지역을 바꾸면 각 칩의 건수가 함께 갱신
- [ ] 0건 카테고리가 `disabled` + `aria-disabled="true"`로 표시되고 클릭해도 빈 목록으로 빠지지 않음
- [ ] 다중 선택이 URL `?cat=korean,snack`에 반영되고 새로고침 후 복원
- [ ] 칩이 `role="group"` 안의 토글 버튼으로 스크린리더에 읽힘

### F3. 식당 목록 (P0)

```
┌──────────────────────────────────────────────────┐
│ 서울 · 강남구 · 일식          총 86곳             │
│                              [가나다순 ▾]        │
├──────────────────────────────────────────────────┤
│  스시오마카세                          [일식]     │
│  서울 강남구 테헤란로 123                         │
│  02-555-0000                          📞  🗺      │
├──────────────────────────────────────────────────┤
│  △△△ 라멘                             [일식]     │
│  서울 강남구 봉은사로 45                          │
│  전화번호 없음                             🗺      │
├──────────────────────────────────────────────────┤
│              ‹ 1 2 3 … 5 ›                        │
│  기준일 2026-07-01 · 출처 문화공공데이터광장       │
└──────────────────────────────────────────────────┘
```

- **총 건수를 항상 상단에** 표기. 이 서비스의 약속은 전수성이므로 숫자가 곧 신뢰다
- 20건/페이지 서버 페이지네이션. 무한스크롤을 쓰지 않는다 — "몇 페이지째인지" 알 수 있어야 하고, URL로 공유 가능해야 한다
- 카드: 상호 / 카테고리 뱃지 / 주소 / 전화. 전화 없으면 **"전화번호 없음"을 명시**(빈칸으로 두면 로딩 실패로 오인)
- 모바일에서 전화번호는 `tel:` 링크
- 좌표가 있으면 🗺 아이콘 → 지도 앱 딥링크 / 상세 이동

**수용 기준**

- [ ] 목록 응답이 캐시 히트 시 200ms, 미스 시 1초 이내
- [ ] 로딩 시 **스켈레톤 카드 5개** (스피너 아님), 레이아웃 시프트 없음
- [ ] 페이지 이동 시 목록 최상단으로 스크롤되고, 포커스가 목록 헤딩으로 이동
- [ ] 결과 0건일 때 "조건에 맞는 식당이 없어요" + **인접 조건 제안**(카테고리 해제 / 상위 지역 보기)
- [ ] 기준일·출처가 모든 목록 화면 하단에 노출

### F4. 식당 상세 (P0)

- 상호, 카테고리, 도로명·지번 주소, 전화, 소개문(템플릿 문구는 숨김)
- **지도** — 좌표가 있을 때만. 없으면 지도 영역 자체를 렌더하지 않는다(빈 회색 박스는 고장으로 보인다)
- 액션: 전화 걸기 / 주소 복사 / 카카오맵·네이버지도로 열기
- 하단: "이 정보는 공공데이터 기준(2026-07-01)이며 실제와 다를 수 있습니다. 방문 전 확인을 권합니다."

**수용 기준**

- [ ] 좌표 결측 시 지도 영역 미렌더 + "위치 정보가 없습니다" 한 줄
- [ ] 주소 복사가 클립보드 API 실패 시에도 폴백(선택 가능한 텍스트)으로 동작
- [ ] 상세 URL이 고유하고 공유 가능 (`/restaurant/{id}`), OG 태그에 상호·주소 반영

### F5. 키워드 검색 (P1)

- 현재 선택된 지역·카테고리 **범위 안에서** 상호명 부분 일치
- 지역 미선택 상태에서는 전국 상호 검색 (상위 50건 + "지역을 좁혀보세요" 안내)

### F7. 내 주변 맛집 찾기 (P0, v0.2 신설)

```
┌──────────────────────────────────────────────────┐
│  📍 내 주변 맛집              [카카오맵 제공]       │
│  반경 [500m] [1km✓] [2km]     카테고리: 전체 ▾     │
├──────────────────────────────────────────────────┤
│              ┌───────────────────┐                │
│              │   ●현위치          │                │
│              │      ● 스시오마카세 │  ← 지도가 메인  │
│              │  ●라멘집           │                │
│              └───────────────────┘                │
├──────────────────────────────────────────────────┤
│  스시오마카세 · 일식 · 320m          📞  🗺        │
│  △△△ 라멘 · 일식 · 480m              📞  🗺        │
│  …최대 45곳                                        │
└──────────────────────────────────────────────────┘
```

- 진입 시 브라우저 Geolocation 권한 요청 → 승인되면 즉시 조회
- **지도가 메인 뷰** (F3 지역목록과 반대 — 거기는 목록이 메인, 지도는 상세에서만). 지도 아래에 리스트가 보조로 붙는다
- 반경 3단계 토글(500m/1km/2km), 카테고리 필터는 §5.3 표준 9종 재사용
- 정렬은 **거리순 고정** — 평점이 없는 서비스이므로 다른 정렬 기준을 제공하지 않는다(F3와 동일 원칙)
- 카드마다 **현재 위치로부터의 거리**를 표기 (예: `320m`, `1.2km`)
- 지역목록(F1~F3)과 지도·카드 UI 컴포넌트는 최대한 공유하되, 데이터 출처 배지("카카오맵 제공" vs "공공데이터 기준 …")로 구분

**수용 기준**

- [ ] 위치 권한 승인 → 결과가 3초 이내 지도에 표시
- [ ] 반경 변경 시 재조회 후 지도가 자동으로 리센터·리줌
- [ ] 카테고리 필터가 F3와 동일한 9종 체계로 동작 (§4.6 매핑 재사용 확인)
- [ ] 결과 카드 클릭 → 지도 마커 하이라이트, 마커 클릭 → 카드 스크롤(양방향 연동)
- [ ] 반경 내 결과가 45건에 걸려 잘린 경우 "이 반경엔 더 많은 곳이 있을 수 있어요 · 반경을 좁혀보세요" 안내 (전수가 아님을 숨기지 않는다)
- [ ] "카카오맵 제공" 배지와 `place_url` 링크가 모든 카드에 노출
- [ ] 위치 권한 거부 시 F1(지역 선택)으로의 명확한 대안 동선 제공

### F6. 예외 처리 & 상태 (P0)

| 상황 | 처리 |
|---|---|
| 로딩 | 스켈레톤 카드 |
| DB 오류 / 타임아웃(3초) | "목록을 불러오지 못했어요" + 재시도 버튼 |
| 결과 0건 | 빈 상태 + 조건 완화 제안 (카테고리 해제 / 상위 지역) |
| 잘못된 지역 슬러그 | 지역 선택 화면으로 폴백, 에러 화면 아님 |
| 잘못된 카테고리 슬러그 | 무시하고 전체로 처리 (에러 아님) |
| ETL 실패로 데이터가 오래됨 | 기준일이 **60일 이상 과거**면 "정보가 오래되었습니다" 배너 |
| 상세 ID 없음 | 404 페이지 + 목록으로 돌아가기 |
| 위치 권한 거부/미지원 (F7) | §4.6 표 참고 — F1 지역 선택으로 유도 |
| 카카오 API 오류/쿼터 초과 (F7) | "지금은 주변 정보를 불러올 수 없어요" + 재시도, F1 폴백 |

---

## 7. 타입 정의

```ts
// ── 공공 API 원본 (모든 필드 문자열, 필드명은 §14-A에서 확정) ──
interface RawRestaurant {
  RSTR_ID?: string;
  RSTR_NM?: string;
  RSTR_RDNMADR?: string;
  RSTR_LNNO_ADRES?: string;
  RSTR_LA?: string;
  RSTR_LO?: string;
  RSTR_TELNO?: string;
  BSNS_STATM_BZCND_NM?: string;
  BSNS_LCNC_NM?: string;
  RSTR_INTRCN_CONT?: string;
}

// ── 정규화 후 내부 모델 = DB 스키마 ──
type Sido =
  | '서울' | '부산' | '대구' | '인천' | '광주' | '대전' | '울산' | '세종'
  | '경기' | '강원' | '충북' | '충남' | '전북' | '전남' | '경북' | '경남' | '제주';

type Category =
  | 'korean' | 'chinese' | 'japanese' | 'western'
  | 'asian' | 'snack' | 'cafe' | 'bar' | 'etc';

interface Restaurant {
  id: string;
  name: string;
  sido: Sido;
  sigungu: string;              // '강남구' | '수원시 영통구' | '세종시'
  category: Category;
  rawBizType: string | null;    // 원본 업태명 — 매핑 개선용으로 보존
  roadAddress: string | null;
  jibunAddress: string | null;
  phone: string | null;         // 정규화된 숫자열
  lat: number | null;
  lng: number | null;
  intro: string | null;         // 템플릿 문구는 null로 저장
  snapshotDate: string;         // ISO date — 이 레코드의 기준일
}

interface RegionOption {
  slug: string;                 // 'gangnam-gu'
  name: string;                 // '강남구'
  count: number;                // 식당 수 (0인 지역은 응답에서 제외)
}

interface CategoryCount {
  category: Category;
  label: string;                // '일식'
  count: number;                // 0이면 UI에서 비활성
}

interface RestaurantListResponse {
  items: Restaurant[];
  total: number;
  page: number;
  pageSize: number;
  categories: CategoryCount[];  // 현재 지역 기준 카테고리별 건수
  snapshotDate: string;
}

// ── F7 전용 — 공공데이터 Restaurant와 분리된 별도 모델 ──
// DB에 영속 저장하지 않는다 (§4.6 라이선스 제약)
interface KakaoNearbyItem {
  kakaoId: string;             // 카카오 장소 ID (자체 저장 안 함 — 응답 그대로 전달)
  name: string;
  category: Category;          // §5.3 매핑 함수로 변환된 표준 카테고리
  rawCategoryName: string;     // 카카오 원본 category_name (디버깅용)
  roadAddress: string | null;
  phone: string | null;
  lat: number;
  lng: number;
  distanceMeters: number;      // 카카오 응답의 distance 필드
  placeUrl: string;            // 카카오맵 상세 페이지 — 항상 노출 필수
}

interface NearbyResponse {
  items: KakaoNearbyItem[];
  center: { lat: number; lng: number };
  radius: number;
  truncated: boolean;          // 45건 상한에 걸렸는지 — true면 UI에 안내 문구
  source: 'kakao';             // Restaurant.source와 구분되는 명시적 플래그
}
```

---

## 8. API 라우트 스펙

| 라우트 | 응답 | 캐시 |
|---|---|---|
| `GET /api/regions` | `RegionOption[]` (시·도 17개 + 건수) | 24시간 |
| `GET /api/regions/[sido]` | `RegionOption[]` (해당 시·군·구) | 24시간 |
| `GET /api/restaurants?sido=&sigungu=&cat=&q=&page=` | `RestaurantListResponse` | 1시간 |
| `GET /api/restaurants/[id]` | `Restaurant` | 24시간 |
| `GET /api/meta` | `{ snapshotDate, totalCount }` | 1시간 |
| `GET /api/nearby?lat=&lng=&radius=&cat=` | `NearbyResponse` (F7, 카카오 프록시) | **60초, geohash 버킷 단위** — §4.6 |

**공통 규칙**

- 데이터가 주 단위로만 바뀌므로 캐시를 길게 잡는다. `Cache-Control: public, s-maxage=3600, stale-while-revalidate=86400`
- 에러 응답: `{ error: { code, message } }`
- **알 수 없는 파라미터는 에러가 아니라 무시**한다. 목록 서비스에서 400은 사용자에게 도움이 안 된다
- `page`는 1-base, 상한 검증 (`page > lastPage`면 마지막 페이지로 클램프)

```ts
// app/api/restaurants/route.ts 골격
export async function GET(req: Request) {
  const p = new URL(req.url).searchParams;
  const sido    = SIDO_BY_SLUG[p.get('sido') ?? ''] ?? null;
  const sigungu = sido ? sigunguFromSlug(sido, p.get('sigungu')) : null;
  const cats    = (p.get('cat') ?? '').split(',').filter(isCategory);
  const page    = Math.max(1, Number(p.get('page') ?? 1));

  const [items, total, categories, meta] = await Promise.all([
    db.findRestaurants({ sido, sigungu, cats, page, pageSize: 20 }),
    db.countRestaurants({ sido, sigungu, cats }),
    db.countByCategory({ sido, sigungu }),   // 카테고리 칩 건수 — cats 필터 제외하고 센다
    db.getMeta(),
  ]);

  return Response.json({ items, total, page, pageSize: 20, categories, snapshotDate: meta.snapshotDate });
}
```

> **`countByCategory`는 카테고리 필터를 빼고 센다.** 필터를 적용해서 세면 선택한 카테고리 외 모든 칩이 0으로 표시되어 다른 카테고리로 이동할 수 없게 된다. 흔한 버그다.

```ts
// app/api/nearby/route.ts 골격 — 카카오 REST 키는 여기서만 사용
export async function GET(req: Request) {
  const p = new URL(req.url).searchParams;
  const lat = Number(p.get('lat'));
  const lng = Number(p.get('lng'));
  const radius = Math.min(Number(p.get('radius') ?? 1000), 2000);
  if (!Number.isFinite(lat) || !Number.isFinite(lng)) {
    return Response.json({ error: { code: 'INVALID_COORDS', message: '좌표가 올바르지 않습니다' } }, { status: 400 });
  }

  const bucket = geohashEncode(lat, lng, 6);           // 짧은 캐시 키 — 좌표 그대로 캐시하지 않는다
  const cached = await shortCache.get(`nearby:${bucket}:${radius}`);
  if (cached) return Response.json(cached);

  const kakaoRes = await fetch(
    `https://dapi.kakao.com/v2/local/search/category.json?category_group_code=FD6&x=${lng}&y=${lat}&radius=${radius}&sort=distance&size=15`,
    { headers: { Authorization: `KakaoAK ${process.env.KAKAO_REST_API_KEY}` } }
  );
  if (!kakaoRes.ok) return Response.json({ error: { code: 'KAKAO_ERROR', message: '주변 정보를 불러오지 못했습니다' } }, { status: 502 });

  const data = await kakaoRes.json();
  const result: NearbyResponse = {
    items: data.documents.map(toNearbyItem),           // category_name → 표준 카테고리 매핑 포함 (§4.6)
    center: { lat, lng },
    radius,
    truncated: data.meta.is_end === false,
    source: 'kakao',
  };

  await shortCache.set(`nearby:${bucket}:${radius}`, result, { ttlSeconds: 60 });  // DB 영속 저장 금지
  return Response.json(result);
}
```

**필수 인덱스**

```sql
CREATE INDEX idx_region_cat ON restaurants (sido, sigungu, category);
CREATE INDEX idx_name_trgm  ON restaurants USING gin (name gin_trgm_ops);  -- F5 검색용
```

---

## 9. 프론트엔드 구조

```
app/
  page.tsx                          # 지역 선택 랜딩
  [sido]/page.tsx                   # 시·군·구 선택
  [sido]/[sigungu]/page.tsx         # 목록 (?cat=)
  restaurant/[id]/page.tsx          # 상세
  nearby/page.tsx                   # F7 — 내 주변 맛집
  api/
    regions/route.ts
    regions/[sido]/route.ts
    restaurants/route.ts
    restaurants/[id]/route.ts
    meta/route.ts
    nearby/route.ts                 # F7 — 카카오 프록시, 키는 여기에만
components/
  RegionPicker.tsx                  # F1 — 캐스케이딩, 모바일 바텀시트
  CategoryChips.tsx                 # F2 — F3·F7 공용
  RestaurantList.tsx                # F3 — 카드 + 페이지네이션
  RestaurantDetail.tsx              # F4
  NearbyMap.tsx                     # F7 — 카카오맵 SDK, 마커·리스트 양방향 연동
  NearbyList.tsx                    # F7 — 지도 아래 보조 리스트
  SourceBadge.tsx                   # "카카오맵 제공" / "공공데이터 기준 …" 구분 표기
  EmptyState.tsx / Skeleton.tsx     # F6
lib/
  db.ts                             # 쿼리
  region.ts                         # 슬러그 ↔ 한글명, 파싱 (§5.2)
  category.ts                       # 매핑 규칙 (§5.3) — F3·F7 공유
  geohash.ts                        # F7 짧은 캐시용 좌표 버킷 (§4.6)
  kakao.ts                          # F7 — 카테고리 검색 호출·응답 정규화 (§4.6)
etl/
  fetch.ts                          # 공공 API 순회 수집
  normalize.ts                      # 주소·카테고리·전화·좌표 정제
  validate.ts                       # ★ 검증 게이트 (§4.5)
  load.ts                           # 스테이징 적재 + 원자적 스왑
.github/workflows/etl.yml           # 주 1회 cron + 수동 트리거
```

**기술 선택**

| 영역 | 선택 | 이유 |
|---|---|---|
| 프레임워크 | Next.js (App Router) | 지역·카테고리별 정적 생성이 가능해 SEO에 유리 — "강남구 일식" 같은 롱테일 검색어가 이 서비스의 자연 유입 경로다 |
| DB | Postgres (Neon / Supabase) | 서버리스, 인덱스 질의로 충분. 20만 행은 작은 편 |
| 쿼리 | Drizzle ORM | 타입 안전 + 생성 SQL이 예측 가능 |
| ETL 실행 | GitHub Actions (cron) | 별도 서버 불필요, 시크릿 관리 내장, 실패 시 알림 |
| 지도 | 카카오맵 JS SDK | 국내 주소·POI 정확도. F4(상세) + F7(주변찾기) 공용 |
| 주변찾기 데이터 | 카카오 로컬 API (REST, `/v2/local/search/category`) | F7 전용, 서버 프록시 경유 (§4.6) |
| 짧은 캐시 | Vercel KV / Upstash Redis | F7 60초 TTL 캐시 — Postgres에는 저장하지 않음(라이선스 제약) |
| 스타일 | Tailwind CSS | |
| 배포 | Vercel | |

**슬러그 규칙**

```
시·도    : 서울 → seoul, 경기 → gyeonggi, 충북 → chungbuk
시·군·구 : 강남구 → gangnam-gu, 수원시 영통구 → suwon-si-yeongtong-gu
```

한글 URL은 인코딩되면 공유 시 깨져 보이므로 **영문 슬러그로 고정**하고, 매핑 테이블은 `lib/region.ts`에 한 벌만 둔다.

**URL 설계 의도**

목록이 `/[sido]/[sigungu]` 경로에 있는 것은 SEO를 위해서다. 쿼리스트링(`?sido=seoul&sigungu=gangnam`)으로 두면 검색엔진이 하나의 페이지로 취급한다. 카테고리는 조합이 많고 다중 선택이므로 쿼리(`?cat=`)에 남긴다.

---

## 10. 구현 순서

1. **문화공공데이터광장 활용신청 → 인증키 발급.** 승인 대기가 있을 수 있으므로 가장 먼저.
2. **`curl`로 실호출 → 응답 JSON을 파일로 저장.** 여기서 §14-A가 해소된다. 확인할 것: 실제 오퍼레이션 경로, 파라미터 목록(지역/업종 필터 존재 여부), 필드명, `totalCount`, `numOfRows` 상한. **이 결과가 §4.2~§4.3을 교체한다.**
   - 만약 **지역·업종 필터 파라미터가 실제로 존재한다면**, §4.4의 결론을 재검토한다. 그래도 건수 집계(F2 칩)와 응답 속도 때문에 ETL 구조가 여전히 유리할 가능성이 높지만, 판단은 실측 후에 한다.
3. `etl/normalize.ts` — 저장한 응답으로 **네트워크 없이 단위 테스트**. 주소 파싱 예외(세종·수원시 영통구·특별자치도)와 카테고리 매핑 20케이스를 고정.
4. `etl/fetch.ts` + `validate.ts` + `load.ts` — **전량 1회 적재 성공**까지. 여기서 미매핑률 리포트를 눈으로 확인하고 규칙을 보강한다.
5. DB 스키마 + 인덱스 + `lib/db.ts` 쿼리. 실제 20만 행에서 목록 질의 응답 시간 측정.
6. API 라우트 5개.
7. `RegionPicker` → `CategoryChips` → `RestaurantList` → 상세 순으로 UI.
8. 예외 상태(F6) — 0건·오래된 스냅샷·DB 오류를 실제로 재현해 확인.
9. GitHub Actions cron 등록 + 실패 알림.
10. **카카오 개발자 앱 등록 → REST API 키 발급 → 도메인 등록.** `curl`로 카테고리 검색 실호출해 응답 필드·`category_name` 계층 구조를 직접 확인 (§4.6 매핑 규칙의 근거 자료).
11. `lib/kakao.ts` + `lib/geohash.ts` — 정규화·카테고리 매핑을 **네트워크 없이 단위 테스트**(저장한 응답 픽스처 사용).
12. `/api/nearby` 프록시 + 짧은 캐시 연동 → `NearbyMap`/`NearbyList` UI(F7). 위치 권한 거부·오류 흐름을 실제로 재현해 확인.
13. 접근성(키보드, 대비비, 스크린리더) → SEO(사이트맵, 구조화 데이터) → Lighthouse.

> **4번에서 가장 오래 걸린다.** 주소 파싱과 카테고리 매핑은 실제 데이터를 보면서 반복 수정해야 한다. 미매핑률 리포트를 보고 규칙을 고치는 사이클을 여러 번 돌 각오로 일정을 잡는다.

---

## 11. 성공 지표

| 지표 | 목표 | 의미 |
|---|---|---|
| 지역 선택 완료율 | 70% 이상 | 진입 후 시·군·구까지 고르는가 — 낮으면 F1 UX 문제 |
| 세션당 카테고리 전환 수 | 평균 1.5회 이상 | 목록을 실제로 탐색하는가 |
| 상세 진입률 | 세션의 40% 이상 | 목록이 쓸모 있는가 |
| 검색엔진 유입 비중 | 30% 이상 | 롱테일 SEO 전략의 검증 |
| 카테고리 `etc` 비율 | 15% 미만 | 매핑 규칙 건강도 |
| 시·군·구 미매핑률 | 1% 미만 | 주소 파싱 건강도 |
| **F7 위치 권한 승인율** | 60% 이상 | 낮으면 F1 동선 강화 필요 |
| **F7 → F4/카카오 상세 진입률** | 세션의 30% 이상 | 주변찾기 결과가 실제로 쓸모 있는가 |
| LCP | 2.5초 이내 | |

---

## 12. 로드맵

| 단계 | 범위 |
|---|---|
| **v1.0 (MVP)** | F1~F4, F6 + 반응형 + 주 1회 ETL |
| **v1.1** | 목록 지도 뷰, 현재 위치 기반 자동 선택, 즐겨찾기(localStorage), 키워드 검색(F5), 인허가 업종 보조 필터 |
| **v1.2** | 사용자 제보(폐업/정보 오류), 다른 공공 데이터셋 병합(관광공사 음식점, 지자체 모범음식점 인증) |
| **v2.0** | 지역별 통계(카테고리 분포·밀집도), 여행 코스 저장, 다국어(외국인 관광객) |

---

## 13. 리스크

| 리스크 | 영향 | 대응 |
|---|---|---|
| **API 실제 규격이 가정과 다름** | 설계 전면 재검토 | 구현 순서 2번을 **다른 모든 코드보다 먼저** 수행. 파서 이후 코드는 실측 전에 쓰지 않는다 |
| **지역/업종 필터 파라미터 부재** | "선택 후 조회"가 불가 | ETL 구조로 이미 대응(§4.4). 오히려 이 전제가 설계의 출발점 |
| 주소 파싱 실패 | 지역별 목록에 구멍 | 예외 케이스 테스트 고정, 미매핑률 게이트(3%), 법정동코드 조인으로 승급 경로 확보 |
| 카테고리 오분류 | 잘못된 목록 | 규칙 순서 테스트, `etc` 리포트 상시 확인, 원본 업태명 DB 보존 |
| **폐업·오래된 정보** | 헛걸음 → 신뢰 상실 | 기준일 상시 표기, 상세 하단 고지, v1.2 제보 기능 |
| ETL 실패 / 스키마 변경 | 데이터 갱신 중단 | 검증 게이트 + 원자적 스왑 → **깨진 데이터 대신 이전 스냅샷 서빙**, Actions 실패 알림 |
| 호출 한도 초과 | ETL 미완료 | 순차 수집 + 200ms 간격, 전량이 한도의 20% 이내(§4.4), 중단 지점부터 재개 |
| 데이터 재배포 라이선스 | 서비스 지속성 | 공공누리 유형 확인 후 출처 표기 준수. 상세·푸터에 출처·기준일 명시(§14-D) |
| 인증키 유출 (공공데이터) | 키 폐기·재발급 | 키는 **Actions 시크릿에만** 존재. 웹 런타임은 공공 API를 호출하지 않음 |
| **카카오 REST 키 유출** | 쿼터 도용, 서비스 오작동 | 서버 프록시(`/api/nearby`)에서만 사용, 클라이언트 미노출. 도메인 화이트리스트도 콘솔에 등록 |
| **F7 결과 영속 저장 (약관 위반)** | 계정 정지 | §4.6 원칙 — DB 테이블 저장 금지, 초 단위 캐시만. 코드리뷰 체크리스트에 포함 |
| **F1~F3과 F7 데이터 혼동** | "왜 여기엔 있는데 저기엔 없지" 신뢰 저하 | `SourceBadge` 컴포넌트로 모든 화면에서 출처·기준일 강제 표기 (§4.6) |
| DB 비용 | 운영 부담 | 20만 행은 무료 티어 범위. 원본 아카이브는 DB가 아닌 오브젝트 스토리지/리포지토리에 |

---

## 14. 미결 항목

| # | 항목 | 결정 필요 사항 |
|---|---|---|
| **A** | **API 실제 규격** | 오퍼레이션 경로, 파라미터(지역·업종 필터 존재 여부), 응답 필드명, `totalCount`, `numOfRows` 상한, 응답 형식. **구현 순서 2번에서 실호출로 확정.** §4.2~§4.3 전체가 이 결과로 교체된다 |
| **B** | 영업시간·휴무일 필드 | 원본에 있는지 확인. 있으면 F4에 추가, 없으면 "영업시간 정보 없음"을 명시 |
| **C** | 행정구역 테이블 도입 | 미매핑률이 게이트를 넘으면 법정동코드 조인으로 전환 — 번들·DB가 커진다 |
| **D** | 라이선스 | 공공누리 유형 확인, 출처 표기 문구 확정, 상업적 이용·재배포 가능 범위 |
| **E** | 갱신 주기 | 원본 데이터의 실제 갱신 주기 확인 후 cron 조정 (주 1회가 과할 수 있음) |
| **F** | 서비스명 & 브랜딩 | "동네한끼"는 가칭 |
| **G** | 지도 SDK | 카카오맵 vs 네이버지도 — 키 발급 절차·무료 한도 비교 |
| **H** | 다크 모드 | v1 포함 여부 |
| **I** | 카카오 로컬 API 쿼터 | 무료 한도(일 30만 건 내외로 알려짐) 실측 확인 및 트래픽 급증 시 유료 전환 기준 |
| **J** | 카카오 vs 공공데이터 카테고리 정확도 비교 | §4.6 매핑이 실제 카카오 응답에서 `etc` 비율이 얼마나 나오는지 실측 후 규칙 보강 필요 여부 판단 |

---

### 참고 자료

- [문화공공데이터광장 — 오픈API 안내](https://www.culture.go.kr/data/openapi/openapiInfo.do)
- [문화공공데이터광장 — 맞춤형 API 목록](https://www.culture.go.kr/data/openapi/openapiList.do?category=I)
- [한국문화정보원 — 공공데이터 개방](https://www.kcisa.kr/kr/contents/open_openData/view.do)
- [공공데이터포털](https://www.data.go.kr)
- [공공누리 — 공공저작물 자유이용 허락 표시](https://www.kogl.or.kr/)
- [카카오 개발자 — 로컬 API 개발 가이드](https://developers.kakao.com/docs/latest/ko/local/dev-guide)
- [카카오 개발자 — 카테고리로 장소 검색](https://developers.kakao.com/docs/latest/ko/local/dev-guide#search-by-category)
